before running the file please run the following command in terminal from this directory

if pip is not installed, please install pip before running the following command 



      pip install -r requirements.txt
